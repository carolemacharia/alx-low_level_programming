#include "main.h"
#include <stdio.h>
/*
*main checks all code and calls functions
*Return: Always 0.
*/
int main(void)
{
char c;
c = 'A';
printf("%c: %d\n", c, _isupper(c));
c = 'a';
printf("%c: %d\n", c, _isupper(c));
return (0);
int _isdigit(int c);
return(0);
int mul(int a, int b);
return(0)
}
